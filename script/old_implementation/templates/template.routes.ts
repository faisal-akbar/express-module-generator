import { Router } from "express";
import { __NAME__Controllers } from "./__FILENAME__.controller";

const router = Router();

// Example route
// router.get("/", __NAME__Controllers.exampleMethod);

export const __FILENAME__Routes = router;
